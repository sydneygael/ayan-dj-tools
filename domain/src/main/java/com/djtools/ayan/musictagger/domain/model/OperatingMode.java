package com.djtools.ayan.musictagger.domain.model;

/** Mode de fonctionnement : PLAN (revue batch), MANUAL (confirmation un par un), APPLY (auto). */
public enum OperatingMode { PLAN, MANUAL, APPLY }
